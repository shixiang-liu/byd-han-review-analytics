# Dependencies and build output
node_modules/
dist/
build/
.venv/
venv/
__pycache__/
*.py[cod]
.pytest_cache/
coverage/

# Data and model artefacts (large, third-party, or regenerable).
# Note: `data/` is anchored to the repository root so that the dashboard's own
# inputs under frontend/public/data/ stay tracked.
/data/
/processed_data/
*.pt
*.bin
*.safetensors
/models/
/outputs/

# Local secrets and IDE state
.env
.env.local
.idea/
.vscode/
.DS_Store
Thumbs.db
*.log
